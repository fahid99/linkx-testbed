# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Only the agent chain (agents.run_trial) needs this; the API and MCP
# servers do not call Anthropic.
ANTHROPIC_API_KEY=sk-ant-...
